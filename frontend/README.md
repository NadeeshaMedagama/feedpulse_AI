# Frontend (Next.js)

Contains:
- Public feedback submission page (`/`)
- Admin dashboard (`/dashboard`) with login, filters, search, sort, pagination, sentiment badges, status updates, stats, and AI summary display.

## Run
```bash
npm run dev --workspace frontend
```

Frontend URL: `http://localhost:3030`
Admin dashboard URL: `http://localhost:3030/dashboard`

