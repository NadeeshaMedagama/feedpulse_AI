{
  "name": "@feedpulse/ai-service",
  "version": "1.0.0",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "dev": "node --env-file=../../.env --watch src/server.js",
    "start": "node --env-file=../../.env src/server.js",
    "start:docker": "node src/server.js",
    "test": "NODE_ENV=test jest --runInBand"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "jest": "^30.3.0"
  }
}
