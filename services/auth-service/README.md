# Auth Service

Simple admin authentication service with hardcoded credentials from environment variables.

## Endpoint
- `POST /auth/login`

Returns JWT token used by the gateway to protect admin routes.

